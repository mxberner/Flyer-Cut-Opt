# Cache

This directory stores local runtime cache files that are not source-of-truth records.

- `stack_id_cache.json`: locally reserved stack IDs used for automatic assignment when a run-config leaves `ID` blank or sets it to `auto`.
